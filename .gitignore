# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
*.tsbuildinfo

# Environment and secrets (never commit)
.env
.env.local
.env.*.local

# Logs and debug
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build / deploy reports (generated locally)
build_report*.txt

# Vercel (deploy config; use project settings instead)
.vercel/

# Editor and OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Optional: keep only if you don't want to track lockfiles at GPS root
# GPS/package-lock.json
